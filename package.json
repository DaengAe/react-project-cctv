{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "heroku-prebuild": "cd my-app && npm ci && npm run build && move build ..\\server && move ..\\JsonData ..\\server\\build",
    "start": "cd server && npm ci && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  }
}
